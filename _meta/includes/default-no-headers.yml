documentclass: scrartcl
toc: true # Enable the Table of Contents
toc-title: "Index"
toc-depth: 3 # Include headings up to level 3 (###)
number-sections: true
sitemap: true # Enables sitemap generation for web crawlers
tbl-colwidths: auto
license: "EUPL (>= 1.2)"
keywords: "{keywords_str}"
format:
  html:
    include-in-header:
      - ../_meta/includes/json-ld.html
    include-before-body: ../_meta/includes/default-before-body.html
    css: _meta/theme/styles.css
    code-fold: true # Allow code blocks to be foldable
    self-contained: false # Don't embed resources like CSS and images
    filters:
      - ../../.github/scripts/filters/extract-images.lua
      - ../../.github/scripts/filters/inject_contact_info.lua
      - ../../.github/scripts/build/inject_changelog.py

  # PDF output: rendered by Typst via a custom OFL-licensed template.
  typst:
    template-partials:
      - _meta/theme/typst/typst-template.typ
      - _meta/theme/typst/typst-show.typ
    toc-title: "Table of contents"
    section-numbering: 1.1.1
    papersize: a4
    margin:
      x: 2.54cm
      top: 2.54cm
      bottom: 3.35cm
    fontsize: 10pt
    # Contact info isn't injected here for Typst - typst-template.typ renders it
    # after the TOC so paging stays clean. No image-description filter either:
    # PDFs are untagged so fig-alt does nothing (it goes in html alt + .llms.md).
    filters:
      - ../../.github/scripts/build/inject_changelog.py

  # gfm writer makes the .llms.md sidecars. Descriptions are baked into the
  # source by inject_image_descriptions.py, so no filter is needed here.
  gfm: default

format-links:
  - html
  - format: typst
    text: PDF
    icon: file-pdf
